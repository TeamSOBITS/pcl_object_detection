/**/preprocessor:
  ros__parameters:
    input_topic: "/sobit_home/head_camera/depth/points"
    output_topic: "cloud_filtered"
    base_frame: "base_footprint"

    # Passthrough Filter
    voxel_leaf_size: 0.02
    x_min: 0.2
    x_max: 2.0
    y_min: -1.0
    y_max: 1.0
    z_min: 0.0
    z_max: 1.0
